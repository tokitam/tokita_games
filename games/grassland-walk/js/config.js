export const CONFIG = {
  worldSize: 512,
  walkSpeed: 2.2,
  runSpeed:  5.8,
  accelTau:  0.15,
  decelTau:  0.10,
  turnRate:  10,
  charRadius: 0.35,
  charHeight: 1.7,
  maxDt: 1 / 30,

  camera: {
    fov: 60,
    near: 0.3,
    far:  500,
    distance:    4.5,
    minDistance: 2,
    maxDistance: 12,
    initElev: (18 * Math.PI) / 180,
    minElev:  (-5 * Math.PI) / 180,
    maxElev:  (75 * Math.PI) / 180,
    sensitivity: 0.005,
    rotTau:   0.08,
    pivotTau: 0.05,
    distTau:  0.15,
    occlRecoverTau: 0.3,
    pivotHeight:   1.5,
    collisionPad:  0.3,
    minOccl:       0.6,
  },

  fogNear: 80,
  fogFar:  320,

  nature: {
    bigTreeCount:   40,
    smallTreeCount: 80,
    grassCount:    400,
    flowerCount:   200,
    rabbitCount:     6,
    squirrelCount:   4,
    butterflyCount: 10,
  },
};
